# projetrte
Cette branche est celle sur laquelle seront faite les Merges Request(MR). donc pour chacune des équipes, une fois créer vos branches respective, toutes vos commit seront faites sur ces branches la et les MR sur celle ci(dev).
